<Window x:Class="Client.Views.MainChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Title="Fowa-Chat" Height="378" Width="606.93">
        
    <Grid x:Name="MainChatWindow1">
        <TabControl x:Name="ChatTab" ItemsSource="{Binding UserChats}" IsSynchronizedWithCurrentItem="True">
            
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <!--<local:ChatUserControl HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"></local:ChatUserControl>-->
                </DataTemplate>
            </TabControl.ContentTemplate>

            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Header" Value="{Binding Nick}" />
                </Style>
            </TabControl.ItemContainerStyle>
            
        </TabControl>
    </Grid>
</Window>
