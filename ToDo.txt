--------------------
Client: (MVVM)
--------------------

	- Gui Redesign (First at all learn to understand the MVVM Pattern :D)
		- ViewModels
		- MainChatWindow with Tabs
		- Implement IDataErrorInfo to RegisterViewModel (DataTrigger, e-mail regex etc.)
	
	-Add resource file for messages (done)
	-Add settings file (done)
	-Add TabView using CaliBurn Micro Sreen/Conductor
	-Implement dependency injection for a service to open and close views from code.
	- ...

--------------------
FowaProtocol:
--------------------
	
	- Create XML-Messages with an API (Header, Body, ...) // done
	- Write a ConnectionCach class
	- rewrite the Connect Method to use the Socket methode ConnectAsync() because of reconnectin issues
	  (If the socket has been previously disconnected, then you cannot use this method to restore the connection) 
	- ...

--------------------
Server:
--------------------

	- Implement RepositoryPattern (done)
	- NAT Traversal (maybe not nessecary through the architecture)
	- Implement a list with connected cients
	- Implement a way of notify if a user is on-/offline 
	- ...

--------------------
- Tests to perform:
--------------------

	- FowaClient
	- FowaService (events etc.)
	- Repositories / Services
	- ...

#######################################################################################################################################

Additional notes:
	
	- Maybe to much logic within the Messages-Ctors. ( matter of taste :) )
	- Can not use the DbContext for multi accessing. Mabye try to create a copy of the DbContext and after an action discard this copy.
	  I will need this in order to not have to use the locking statement.